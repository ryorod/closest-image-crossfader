# closest-image-crossfader

Search the closest image to the query image from a local dataset and sends the path to the maxpatch over OSC.
xfade.maxpat crossfades the query image and the closest image.
